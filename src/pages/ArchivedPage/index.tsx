import React from 'react';
import { Typography } from '@mui/material';

const ArchivedPage: React.FC = () => {
  return (
    <div>
      <Typography variant="h4">Archived</Typography>
      <p>View archived items here.</p>
    </div>
  );
};

export default ArchivedPage;
